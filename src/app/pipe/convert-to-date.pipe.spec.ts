import { ConvertToDatePipe } from './convert-to-date.pipe';

describe('ConvertToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
